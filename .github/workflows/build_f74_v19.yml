name: sports physics renderer v3
on:
  push:
  workflow_dispatch:

jobs:
  build_app:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build renderer core
        run: |
          SDK=$(xcrun -sdk iphoneos --show-sdk-path)
          xcrun -sdk iphoneos clang -arch arm64 -mios-version-min=17.0 -O2 -fobjc-arc \
            -o App campaigns/s159_apple/heap_fengshui_v19.m \
            -isysroot "$SDK" \
            -framework UIKit -framework WebKit -framework Foundation \
            -framework Metal -framework CoreFoundation \
            -framework IOKit
          file App
          ls -la App
          echo "Build hash: $(shasum -a 256 App | cut -d' ' -f1)"
      - uses: actions/upload-artifact@v4
        with:
          name: game-binary-v19
          path: App

  package_app:
    runs-on: macos-latest
    needs: build_app
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: game-binary-v19
      - name: Package IPA
        run: |
          mkdir -p App.app
          cp App App.app/App
          chmod +x App.app/App

          cat > App.app/Info.plist << 'PLIST'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
            "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0"><dict>
            <key>CFBundleExecutable</key><string>App</string>
            <key>CFBundleIdentifier</key><string>com.nexus.sportsphysics3</string>
            <key>CFBundleVersion</key><string>3.0</string>
            <key>CFBundleShortVersionString</key><string>3.0</string>
            <key>CFBundleName</key><string>SportsPhysics_v3</string>
            <key>CFBundlePackageType</key><string>APPL</string>
            <key>UIRequiredDeviceCapabilities</key><array><string>arm64</string></array>
            <key>MinimumOSVersion</key><string>17.0</string>
            <key>LSRequiresIPhoneOS</key><true/>
          </dict></plist>
          PLIST

          codesign -f -s - App.app/App
          mkdir -p Payload
          cp -r App.app Payload/
          zip -r fengshui_v19_adhoc.ipa Payload/
          ls -la fengshui_v19_adhoc.ipa
      - uses: actions/upload-artifact@v4
        with:
          name: fengshui_v19_adhoc
          path: fengshui_v19_adhoc.ipa
